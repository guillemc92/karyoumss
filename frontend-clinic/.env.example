VITE_CLINIC_API_BASE=/api/clinic
VITE_USE_MSW=false
# SSO (ADR-0020, DD-SSO-001 §4.1): dejar vacío para dev normal (:5174 directo).
# Setear a /clinic/ solo si se sirve detrás de Caddyfile.dev (npm run dev con
# VITE_BASE_PATH=/clinic/ npm run dev, o vía script dedicado).
VITE_BASE_PATH=
