# backend-admin — Django + DRF (ADR-0013)
# Bootstrap del bounded context admin. Stack separado del clínico (FastAPI).

# Core
Django==5.0.6
djangorestframework==3.15.2

# Auth & RBAC
django-guardian==2.4.0                    # RBAC per-object permissions

# Audit Append-Only
django-auditlog==3.0.0                    # Signals Django → LogEntry

# JWT validation (F0 auth bridge)
PyJWT==2.9.0                              # HS256 validation de FastAPI JWT

# PostgreSQL driver
psycopg[binary]==3.2.1                    # psycopg3 (recomendado Django 5)

# Settings & config
python-dotenv==1.0.1                       # Carga .env

# Rate limiting
django-ratelimit==4.1.0                   # 60 req/min por user

# CORS (F4 frontend-admin separado)
django-cors-headers==4.4.0

# Production server
gunicorn==22.0.0                          # WSGI server

# Tests (RN-09)
pytest==8.3.2
pytest-django==4.9.0
pytest-cov==5.0.0
factory-boy==3.3.0                        # Fixtures

# Linting (opcional pero recomendado)
ruff==0.5.5

# Constraints: Django 5.0+ requiere Python 3.10+. Probado con Python 3.12.